# Supabase Configuration
# Copy this file to .env and fill in your actual values

# Your Supabase project URL
SUPABASE_URL=https://your-project-id.supabase.co

# Your Supabase anonymous (public) key
# This key is safe to use in client-side applications
# It's protected by Row Level Security (RLS) policies in your database
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# IMPORTANT SECURITY NOTES:
# 1. NEVER commit the actual .env file to git (it's in .gitignore)
# 2. NEVER use SUPABASE_SERVICE_ROLE_KEY in client-side code
# 3. Always enable Row Level Security (RLS) on your database tables
# 4. The ANON key is meant to be public but still keep it in .env
# 5. Share this .env.example file, NOT the actual .env file

# How to get your keys:
# 1. Go to https://supabase.com/dashboard
# 2. Select your project
# 3. Go to Settings > API
# 4. Copy the Project URL and anon/public key
